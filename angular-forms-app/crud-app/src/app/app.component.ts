import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})


export class AppComponent {
  title = 'crud-app';
  
}










/*

    "Comment":"To run this JSON file From Out side we need to install JSON Server globlly 'npm install g json-server'",
          "com" :"to run json we need change open cmd and run json with new port numner",
          "comm": "json-server -p 4500 filename",

*/